<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zyt.springbootlearning.dao.NodeMapper">

    <select id="getNode" parameterType="String" resultType="cn.zyt.springbootlearning.domain.Node">
        select name, capacity, cores from tb_node where name = #{name}
    </select>

</mapper>